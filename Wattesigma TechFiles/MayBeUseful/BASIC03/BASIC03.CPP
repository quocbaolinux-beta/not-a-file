#include <bits/stdc++.h>

using namespace std;
long long t, n, m, kq;
int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL); cout.tie(NULL);
    freopen("BASIC03.INP", "r", stdin);
    freopen("BASIC03.OUT", "w", stdout);
    cin >> t >> n >> m;
    kq = (t*3) + (n*2) + (m*5);
    cout << kq;
    return 0;
}
