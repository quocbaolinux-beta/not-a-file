#include <bits/stdc++.h>
using namespace std;
long long a, t, kq;
int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL); cout.tie(NULL);
    freopen("BASIC02.INP", "r", stdin);
    freopen("BASIC02.OUT", "w", stdout);
    cin >> a >> t;
    kq = a*t;
    cout << kq;
    return 0;
}
