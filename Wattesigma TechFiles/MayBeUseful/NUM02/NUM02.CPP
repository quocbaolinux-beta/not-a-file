#include <bits/stdc++.h>
using namespace std;

long long n, kq;
int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL); cout.tie(NULL);

    freopen("NUM02.INP", "r", stdin);
    freopen("NUM02.OUT", "w", stdout);

    cin >> n;
    kq = (n/10)%10;54
    cout << kq;
    return 0;
}
