#include <bit/stdc++.h>
using namespace std;

long long a, b, c;
int main()
{
    ios_base::sync_with_stdio(false)
    cin.tie(NULL); cout.tie(NULL)

    freopen("001.INP","r",stdin)
    freopen("001.OUT","w",stdout)
    cin >> a >> b;

    c=a+b;

    cout << c << endl;
    return 0;
}
